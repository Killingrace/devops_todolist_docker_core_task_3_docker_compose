# Docker Compose todo app

This project contains Python Django todo app.

## Running app

to run app enter the following command in terminal in app directory:

```bash
docker compose up -d
```

## Accessing the app

To open the app follow the [link](http://localhost:8080)

## Stopping the app

to stop app enter command in terminal:

```bash
docker compose down
```